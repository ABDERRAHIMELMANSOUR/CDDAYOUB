{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "vite",
  "rewrites": [
    {
      "//": "The negative lookahead is deliberate even though there is no api/ directory today. A plain /(.*) swallows any serverless function added later: the request returns index.html with a 200, the browser reads that as success, and submissions are discarded while the form says thank you. That happened once already.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
